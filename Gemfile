# define our source to loook for gems
source "http://rubygems.org/"

# declare the sinatra dependency
gem 'sinatra'
gem 'multi_json'
gem 'facter'

group :puppet do
  gem 'puppet'
end

group :test do
  gem 'rspec'
end
